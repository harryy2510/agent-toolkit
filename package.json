{
	"name": "@harryy/agent-toolkit",
	"version": "0.1.32",
	"description": "Fast agent-agnostic setup, repo intelligence, and enforcement CLI.",
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "https://github.com/harryy2510/agent-toolkit"
	},
	"bin": {
		"agent-toolkit": "./bin/agent-toolkit.ts",
		"atk": "./bin/agent-toolkit.ts"
	},
	"files": [
		"bin",
		"src",
		"crates",
		"Cargo.toml",
		"README.md",
		"AGENTS.md",
		".claude-plugin"
	],
	"type": "module",
	"scripts": {
		"build:native": "bun scripts/build-native.ts",
		"test": "bun test",
		"test:rust": "cargo test",
		"check": "bun test && cargo test && oxlint --type-aware --type-check bin src scripts test",
		"format": "oxfmt"
	},
	"devDependencies": {
		"@types/bun": "^1.3.14",
		"oxfmt": "^0.51.0",
		"oxlint": "^1.66.0",
		"oxlint-tsgolint": "^0.23.0"
	},
	"engines": {
		"bun": ">=1.2.0"
	}
}
